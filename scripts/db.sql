
CREATE TABLE public.mock_data (
	id varchar NOT NULL,
	nombre varchar NOT NULL,
	tipo varchar NULL,
	region varchar NULL,
	ataque varchar NULL,
	valor varchar NULL,
	CONSTRAINT mock_data_pkey PRIMARY KEY (id)
);

